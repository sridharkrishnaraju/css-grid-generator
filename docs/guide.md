# Guide: Grid vs Flexbox

Use Flexbox for one-dimensional layouts (a single row or column) and CSS Grid for two-dimensional ones (rows and columns together). They compose well — a grid of cards where each card is a flex column is a clean, common pattern.

## Beyond this tool

This is one of several free developer tools from
[SGBP — Singapore Build Partners](https://sgbp.tech). If you'd like a full website built or
audited for performance, accessibility and SEO rather than a single utility, that's what the
studio does for Singapore SMEs.
